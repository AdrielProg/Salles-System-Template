@{
    ViewData["Title"] = "Login";
}

<div id="partialViewContainer">
    @await Html.PartialAsync("_Login")
</div>

<a href="javascript:void(0);" onclick="login.loadPartialView('@Url.Action("RenderPartialView", "Account")', 'Register')">Register</a>
<a href="javascript:void(0);" onclick="login.loadPartialView('@Url.Action("RenderPartialView", "Account")', 'ChangePassword')">Change Password</a>

@section Scripts {
    <script src="/js/login.js"></script>
}
